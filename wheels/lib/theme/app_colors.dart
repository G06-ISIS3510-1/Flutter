import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const primary = Color(0xFF1A3A5C);
  static const primaryForeground = Color(0xFFFFFFFF);
  static const primaryLight = Color(0xFF2D5280);

  static const accent = Color(0xFF00D9A3);
  static const accentForeground = Color(0xFF1A1F36);
  static const accentHover = Color(0xFF00C794);

  static const secondary = Color(0xFF5B89C8);
  static const secondaryForeground = Color(0xFFFFFFFF);
  static const secondaryLight = Color(0xFFE8F0F9);

  // State
  static const success = Color(0xFF00D9A3);
  static const warning = Color(0xFFFFA726);
  static const error = Color(0xFFFF5252);
  static const info = Color(0xFF42A5F5);

  // Base UI
  static const background = Color(0xFFFFFFFF);
  static const foreground = Color(0xFF1A1F36);
  static const card = Color(0xFFFFFFFF);
  static const cardForeground = Color(0xFF1A1F36);
  static const border = Color(0xFFE5E9F2);
  static const input = Color(0xFFF7F9FC);
  static const muted = Color(0xFFF7F9FC);
  static const mutedForeground = Color(0xFF64748B);

  // Compatibility aliases
  static const surface = card;
  static const textPrimary = foreground;
  static const textSecondary = mutedForeground;
}
